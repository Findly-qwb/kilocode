---
"@kilocode/desktop": minor
---

Add kilo-desktop: a Tauri desktop client for the kilo serve backend — chat with streaming responses, file/image attachments, message edit/retry, permission approval and follow-up questions; pick models, reasoning variants and assistants; manage providers (API key + OAuth, coding-plan grouping), skills, MCP servers (add/connect/remove) and slash commands; browse project files with thumbnails and session artifacts; settings with themes, default model/assistant and backend restart.

