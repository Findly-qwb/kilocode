---
"@kilocode/desktop": minor
---

Add kilo-desktop: a Tauri desktop client for the kilo serve backend — chat with streaming responses and permission approval, manage providers (API key + OAuth), and browse skills, MCP servers, and slash commands.
